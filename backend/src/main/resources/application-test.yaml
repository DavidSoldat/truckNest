spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dummy-auth/realms/trucknest
          jwk-set-uri: https://dummy-auth/protocol/openid-connect/certs

brevo:
  from-email: test@test.com

keycloak:
  admin:
    client-secret: dummy-secret